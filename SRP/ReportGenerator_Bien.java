package SRP;

public class ReportGenerator_Bien {
	public void generateReport(Report_Bien report) {
		// Genera el reporte en un formato específico
		System.out.println("Generating report: " + report.getTitle());
		}
}
